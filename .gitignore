.claude/settings.local.json
.claude/deploy.log
*.log
.DS_Store

# Astro build output / generated types
dist/
.astro/

# dependencies
node_modules/

# environment variables
.env
.env.production

# editor
.idea/

# terraform — local working files, never committed; .terraform.lock.hcl
# IS committed (pins provider versions) so it's deliberately not listed here
terraform/.terraform/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/plan.out
terraform/*.tfvars
